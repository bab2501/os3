#!/usr/bin/env python3
#Credit to: https://www.os3.nl/2017-2018/students/ivo_van_der_elzen/inr/lab3#task_8 
import sys
 
first = sys.argv[1]
last = sys.argv[2]
 
print( "IPv4: " + str(len(first)) + "." + str(len(last)) + ".0.0/17" )
 
print( "IPv6: 2001:0db8:0" + hex(sum([ord(x) for x in first]) % 16)[2::] + "00:0" + hex(sum([ord(x) for x in last]) % 16)[2::]  + "00::1/58" )

